# 顾明泱作品档案网站设计系统

## 首页重构判断

- 可直接采用：固定四段页眉、三栏身份网格、138px 项目行、右侧分类条、悬停浮窗的定位与开合机制。
- 不直接复制：AB/GD 名称、个人履历、项目名称、原站图片、受保护字体与原始代码。
- 改造后采用：身份替换为 GU/MY；首页不嵌入项目图片，浮窗改为空黑窗口；完整作品图移动到独立 Archive 页面。
- 证据不足：参考站内部滚动与部分入场动画的精确缓动无法从单张截图确认，因此采用实测后的原生滚动与 420ms 浮窗过渡。

首页核心桌面令牌：2048px 视口时页边距 33px、空窗 317×244px、项目行 138px、浮窗 317×476px；浮窗锚定当前项目行右侧 16.8%，并向上偏移自身高度的 25%。

内容语言采用“中文叙事 + 英文标签”：标题、介绍与项目说明以中文为主，`INTRO / INFOGRAPHIC / PREVIOUS` 等英文只作为较小的信息层，避免整页英文削弱个人表达。

首页保留左侧留白，中间 31.75vw 区域以五条纵向履历摘要呈现教育、学业、荣誉、竞赛与科研；右侧 31.75vw 区域使用黑白人物照片。信息与照片共享相同上下边界，以 1px 分隔线建立理性秩序；手机端先显示履历，再显示照片。

项目索引移除无内容的“近期进行中”占位，列表紧接身份首屏；悬停浮窗使用每个项目对应的正式封面。设计方法以“研究→洞察、探索→实验、定义→构建、验证→演进”建立从旧词到行动性新词的转译关系。

## 项目详情模板

- 可直接采用：固定 41px 页眉、1000px 双栏首屏、三段项目说明、下层介绍与上层作品页的滚动覆盖关系，以及前后项目导航。
- 不直接复制：参考项目的品牌内容、图片、英文说明、标志与红色视觉系统。
- 改造后采用：详情页共用一个数据模板，根据项目参数加载顾明泱的名称、说明、年份、独立封面与高清展板；所有展板在纯白前景层中依原作品集顺序连续排列。
- 项目拆分：“金陵雨花石录 · 信息图谱”与“金陵雨花石录 · 交互装置”拥有独立入口、项目编号与详情页；“唐三彩 · 文旅网页设计”作为第十项作品加入档案。
- 证据不足：参考站的自定义平滑滚动参数无法从截图完整确认，因此使用浏览器原生纵向滚动，保留可验证的层叠覆盖结构。

详情页桌面令牌：首屏 1440×1000；右侧视觉 x=726、y=55、691×931；纯白作品层从文档 y=1000 开始，顶部圆角 16px，展板间距 8px；滚动 455px 时作品层上缘位于视口 y=545，覆盖下层介绍。

## 核心判断

新参考站最值得采用的并不是某一张项目图片，而是“把网站当成一本可滚动档案”的机制：固定窄导航、超大无衬线章节标题、贴边项目元信息，以及后续内容覆盖前页的层叠滚动。

## 参考提炼

### 可直接采用

- 41px 固定顶部导航，以及 `MENU / PAGE / TITLE / GRID` 四段式结构。
- `ARCHIVE` 超大无衬线标题和桌面单行、手机拆行的响应方式。
- 项目使用“年份 / 项目名 / 黑底类别标签 / 媒体舞台”的固定模板。
- 项目标题悬停预览锚定在当前行的右侧网格列，15.5vw × 23.25vw，并向上偏移自身高度的 25%。
- 覆盖全页的 GRID 设计网格与 PREV / NEXT 项目导航。
- 大量空白作为档案章节之间的停顿。

### 不直接复制

- 对方的 AB/GD 标识、项目图像、人物身份、联系信息和受保护字体文件。
- 对方网站的原始 Webflow 代码和品牌文案。

### 改造后采用

- `AB/GD` 改为 `GU/MY`，所有项目与年份替换为顾明泱作品集内容。
- 使用系统 Helvetica/Arial/PingFang 字体栈模拟紧凑的新怪诞体比例，无网络字体依赖。
- 虚拟滚动改为原生滚动，保留视觉节奏，同时提高本地文件环境的稳定性与可访问性。

### 证据不足

- 参考站部分图片依赖延迟加载，无法保证复现它每一个媒体转场参数；网站只实现经真实滚轮验证的结构与覆盖机制。

## 设计令牌

- `paper #F3F3F3`：全站浅灰纸张。
- `ink #1A1A1A`：正文、标题、标签和光标。
- `white #FFFFFF`：详情页连续作品层。
- `line #8A8A8A`：1px 档案分隔线。
- 字体：Helvetica Neue / Helvetica / Arial / PingFang SC。
- 小字号信息层统一增加约 `.02em—.045em` 字距，提升中英混排的秩序与可读性。
- 履历、项目说明、方法描述与档案简介采用约 `1.4` 倍行距；大标题继续维持紧凑字面，避免层级失焦。
- 圆角：详情作品层桌面 16px、手机 12px；阴影：0；基础页边距：桌面 23px、手机 14px。

## 对比扫描

主参考提供档案式章节结构；上一版参考站说明固定导航与移动端信息压缩的处理方式；Julia Studio 的项目索引说明大量作品需要持续可见的分类秩序。最终视觉以本次档案参考为唯一主导。

## 响应式与交互

- 桌面：超大 ARCHIVE 单行、项目标题横向三栏、12 栏 GRID 与 14px 基线覆盖层。
- 手机：ARC / HIVE 错位拆行、项目元信息两行重排、GRID 压缩为 6 栏。
- 详情页不进入额外弹窗或横向轮播；全部展板按顺序连续展示，点击 PREV / NEXT 切换项目。
- 雨花石交互装置与自然色差在高清展板末尾追加原始 MP4，详情页与档案轮播均可原位播放。
- 桌面详情首屏使用 sticky 下层，白色作品层以更高层级从底部滚动覆盖；手机端改回自然文档流。
- 桌面端悬停或键盘聚焦项目标题时显示圆角黑底作品浮窗；手机端关闭悬停浮窗，避免遮挡触控内容。
- MENU 打开 376px 页面索引，并把当前页面整体向右推移；Escape 关闭。
- 减少动画偏好会关闭大多数过渡。
